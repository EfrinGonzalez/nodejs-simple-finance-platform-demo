{
  "name": "shine-fintech-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/main.js",
    "dev": "tsx watch src/main.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc -p tsconfig.json --noEmit",
    "seed": "tsx src/scripts/seed-demo.ts",
    "rebuild:projections": "tsx src/scripts/rebuild-projections.ts",
    "worker:outbox": "tsx src/scripts/outbox-worker.ts"
    "rebuild:projections": "tsx src/scripts/rebuild-projections.ts"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "@fastify/swagger": "^9.0.0",
    "@fastify/swagger-ui": "^5.0.0",
    "zod": "^3.23.8",
    "pino": "^9.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "tsx": "^4.19.2",
    "vitest": "^2.1.8",
    "@types/node": "^22.10.2"
  }
}
